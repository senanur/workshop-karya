# Internal reference docs — not part of the shipped project
docs/panduan-infra-pelatihan-web-smp-versi-siswa.md
docs/PHP-404-extended-path-bug.md

# Local data (children's published pages) — never commit real submissions
/data/karya/

# Env / secrets
.env
.env.*

# OS/editor cruft
.DS_Store
Thumbs.db

# Node dependencies (bin/pemutar/ is a build toolchain; the committed artifact
# is aset/scratch/pemutar.js, built once on the dev machine — node_modules must
# never be committed)
node_modules/
